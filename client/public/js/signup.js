/* Takes to 'sign up successful' page after signing up */
document.getElementById("signUp").addEventListener("click", redirectFunction);
function redirectFunction() {
    window.location.href = "#";
}

